kotlin.code.style=official
org.gradle.caching=true
org.gradle.configuration-cache=false
# Build-JVM heap. Default 512 MiB OOMs when CodeQL's extractor traces the Kotlin
# compile against the large IntelliJ SDK classpath; raise it for all builds.
org.gradle.jvmargs=-Xmx4g
